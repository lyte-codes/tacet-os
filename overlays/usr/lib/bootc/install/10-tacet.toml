# bootc install defaults; bootc-image-builder reads these too.
# DECISION: ext4 rather than xfs. Both are fine on a TV box; ext4 can be
# shrunk and inspected with tools that exist on every Linux and macOS host.
[install.filesystem.root]
type = "ext4"
