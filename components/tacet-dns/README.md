# tacet-dns

`blocky` with a bundled blocklist snapshot (spec §4.7). Milestone M3.
**Not started.** Blocklist source and licence is open decision #2.

Already in place for it: `blocky` is installed (not enabled), NetworkManager's
connectivity check is off, and the firewalld `tacet` zone is the default.
